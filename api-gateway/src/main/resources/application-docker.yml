server:
  port: 8080

gateway:
  services:
    finance-base-uri: http://finance-api:8080
    market-base-uri: http://market-data-service:8080

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/finance