spring:
  datasource:
    url: jdbc:h2:mem:market_data_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;NON_KEYWORDS=DAY
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  task:
    scheduling:
      enabled: false

market:
  price-bootstrap:
    enabled: false
  scheduler:
    enabled: false
  fx:
    scheduler-enabled: false
  stock:
    scheduler:
      enabled: false
  bond:
    scheduler-enabled: false
    history-bootstrap:
      enabled: false
    history-refresh:
      enabled: false
  tr-gov-usd-eurobond:
    history-bootstrap:
      enabled: false
    history-refresh:
      enabled: false
  history:
    backfill:
      enabled: false

providers:
  finnhub:
    enabled: false
  fmp:
    enabled: false
