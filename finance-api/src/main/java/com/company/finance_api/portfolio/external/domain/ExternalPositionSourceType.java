package com.company.finance_api.portfolio.external.domain;

/** External pozisyon lot'unun kaynak türünü tanımlayan enum. */
public enum ExternalPositionSourceType {
  MANUAL,
  CSV_IMPORT,
  BROKER_IMPORT
}
